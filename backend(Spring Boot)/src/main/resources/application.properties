server.port=5000

spring.datasource.url=jdbc:mysql://localhost:3306/peergrid?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=cdac

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

jwt.secret=REDACTED_JWT_SECRET
jwt.expiration=3600000
jwt.issuer=PeerGrid
jwt.audience=PeerGridUsers


logging.level.org.springframework.security=DEBUG

# Import secrets
spring.config.import=optional:classpath:application-secrets.properties

# Razorpay Configuration
# razorpay.key.id=${RAZORPAY_KEY_ID}
# razorpay.key.secret=${RAZORPAY_KEY_SECRET}

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ayushranjan2402@gmail.com
spring.mail.password=REDACTED_SMTP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
